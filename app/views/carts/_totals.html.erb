<div>
  <% if @order_items.size == 0 %>
  <div></div>
  <% else %>
  
<div class="container">
      <div id="cart-subtotal" >
      <p class="text-right"><span class="total">Total</span>&nbsp;&nbsp;<span class="total-amount"><%= number_to_currency current_order.subtotal, unit: "€" %></span></p>
      <%= link_to "Continue to payment", new_charge_path, class: 'btn btn-primary cart-submit' %>
      </div>

    </div>
    <% end %>
  
  </div>
